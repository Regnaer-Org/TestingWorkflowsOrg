{
  "name": "Default Codespaces Python DevContainer",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {},
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y micro && pip install -r requirements.txt"
}
